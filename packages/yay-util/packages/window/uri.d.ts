export declare function isUrl(path: string): boolean;
/**
 * 如果符合uri格式
 * 使用decodeUriComponent解码uri
 * @param soa {string|object|Array<String>}
 */
export declare function decodeIfUrl(soa: string | object | Array<String>): any;
